# INTAKE

Intake directory hierarchy.

- `raw/`: Immutable raw input evidence (`chmod a-w`).
- `quarantine/`: Quarantined inputs under evaluation.
- `normalized/`: Normalized intake artifacts with transformation provenance.
