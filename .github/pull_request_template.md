## What changed

<!-- One short paragraph describing the activity. -->

## Why

<!-- Trigger: link the action item, finding, lesson, or decision that raised this work. -->

## Verification

<!-- Gates run and their results. "Tests pass" without counts is not sufficient (CORE-INVARIANT 9). -->

- [ ] `python3 TOOLS/lore_validate.py` — result:
- [ ] Other checks (export, demo, fixtures, etc.) — result:

## Authority

Confirm the following (agent-writable PRs):

- [ ] No author-only status (`accepted`, `normative`, `canonical`, `verified`, `released`, `superseded`, `deleted`) was emitted
- [ ] Nothing under `INTAKE/raw/` was modified
- [ ] No existing files under `SOURCE/` were modified
- [ ] Nothing was promoted, signed, tagged, or published

## Judgment calls

<!-- Choices a reviewer might reasonably reverse. State them plainly. Leave blank if none. -->

## Provenance

<!-- For any derived or generated artifact: input paths + SHA-256 (or commit SHAs). Leave blank if none. -->
