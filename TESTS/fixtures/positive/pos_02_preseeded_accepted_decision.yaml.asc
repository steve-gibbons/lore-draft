REPRESENTATION-ONLY TEST FIXTURE - not a real OpenPGP signature.
The core validator's Check 2 (dependency-free) only confirms this detached-signature
file EXISTS and that provenance.signer_fpr is a registered trusted signer. Real gpg
verification is the CI gate (TOOLS/lore_verify_author_sig.py), which SKIPS TESTS/fixtures
precisely because these carry placeholder signatures. Do not treat this as authority.
